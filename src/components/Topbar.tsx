import { Bell, Command, Download, Moon, Plus, Search, Sun } from 'lucide-react'
import { useEffect, useState } from 'react'
import { useNexus } from '../context/NexusContext'
import { emitUI } from '../lib/ui-events'
type InstallPromptEvent = Event & { prompt: () => Promise<void>; userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }> }
export function Topbar() { const { workspace } = useNexus(); const [dark, setDark] = useState(() => (localStorage.getItem('nexus-theme') || document.documentElement.dataset.theme) !== 'light'); const [installPrompt, setInstallPrompt] = useState<InstallPromptEvent | null>(null); useEffect(() => { document.documentElement.dataset.theme = dark ? 'dark' : 'light'; localStorage.setItem('nexus-theme', dark ? 'dark' : 'light') }, [dark]); useEffect(() => { const handler = (event: Event) => { event.preventDefault(); setInstallPrompt(event as InstallPromptEvent) }; window.addEventListener('beforeinstallprompt', handler); return () => window.removeEventListener('beforeinstallprompt', handler) }, []); async function install() { if (!installPrompt) return; await installPrompt.prompt(); await installPrompt.userChoice; setInstallPrompt(null) } const name = workspace.profile?.display_name || 'Gabriel'; const initials = name.split(' ').map((part) => part[0]).join('').slice(0, 2).toUpperCase(); return <header className="topbar"><button className="command-search" onClick={() => emitUI('command')} aria-label="Abrir central de comandos"><Search size={16} /><span>Pesquisar, abrir ou executar…</span><span className="key-hint"><Command size={12} />K</span></button><div className="topbar__actions"><button className="quick-add-top" onClick={() => emitUI('quickAdd')}><Plus size={15} /><span>Novo</span></button>{installPrompt && <button className="icon-button install-button" onClick={install} aria-label="Instalar Nexus"><Download size={17} /></button>}<button className="icon-button" onClick={() => setDark((value) => !value)} aria-label="Alternar tema">{dark ? <Sun size={17} /> : <Moon size={17} />}</button><button className="icon-button notification-button" onClick={() => emitUI('notifications')} aria-label="Notificações"><Bell size={17} /><span /></button><div className="avatar" title={name}>{initials}</div></div></header> }
