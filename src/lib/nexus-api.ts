import type { User } from '@supabase/supabase-js'
import { supabase } from './supabase'
import type { DailyCheckin, FocusSession, NexusMission, NexusProject, NexusWorkspace, PlayerProfile, Routine, RoutineCompletion, WeeklyReview } from '../types/nexus'

function requireClient() { if (!supabase) throw new Error('Supabase ainda não foi configurado.'); return supabase }
export function localDate(date = new Date()) { return new Intl.DateTimeFormat('en-CA', { timeZone: 'America/Sao_Paulo', year: 'numeric', month: '2-digit', day: '2-digit' }).format(date) }
export async function getCurrentUser() { const client = requireClient(); const { data, error } = await client.auth.getUser(); if (error) throw error; return data.user }
export async function ensureProfile(user: User): Promise<PlayerProfile> { const client = requireClient(); const displayName = (typeof user.user_metadata?.display_name === 'string' && user.user_metadata.display_name) || (typeof user.user_metadata?.full_name === 'string' && user.user_metadata.full_name) || user.email?.split('@')[0] || 'Jogador'; const { data, error } = await client.from('profiles').upsert({ id: user.id, display_name: displayName, updated_at: new Date().toISOString() }, { onConflict: 'id' }).select('*').single(); if (error) throw error; return data as PlayerProfile }
export async function bootstrapNexus() { const client = requireClient(); const { error } = await client.rpc('bootstrap_nexus'); if (error) throw error }
export async function ensureTodayJourney(userId: string) { const client = requireClient(); const today = localDate(); const { data, error } = await client.from('journeys').upsert({ user_id: userId, journey_date: today, title: 'Jornada do dia' }, { onConflict: 'user_id,journey_date' }).select('*').single(); if (error) throw error; return data }
export async function loadWorkspace(userId: string): Promise<NexusWorkspace> { const client = requireClient(); const today = localDate(); const start = new Date(); start.setDate(start.getDate() - 59); const startIso = start.toISOString(); const [profile, missions, projects, journey, checkin, dailyCheckins, focus, routines, completions, activity, attributes, season, rewards, weeklyReviews] = await Promise.all([ client.from('profiles').select('*').eq('id', userId).maybeSingle(), client.from('missions').select('*').eq('user_id', userId).not('status', 'in', '(Feita,Cancelada)').order('due_at', { ascending: true, nullsFirst: false }).limit(40), client.from('projects').select('*').eq('user_id', userId).not('status', 'in', '(Arquivado,Concluído)').order('updated_at', { ascending: false }).limit(20), client.from('journeys').select('*').eq('user_id', userId).eq('journey_date', today).maybeSingle(), client.from('daily_checkins').select('*').eq('user_id', userId).eq('checkin_date', today).maybeSingle(), client.from('daily_checkins').select('*').eq('user_id', userId).gte('created_at', startIso).order('checkin_date'), client.from('focus_sessions').select('*').eq('user_id', userId).gte('started_at', startIso).order('started_at', { ascending: false }).limit(120), client.from('routines').select('*, routine_items(*)').eq('user_id', userId).eq('is_active', true).order('sort_order'), client.from('routine_completions').select('id,routine_item_id,completion_date').eq('user_id', userId).eq('completion_date', today), client.from('activity_events').select('*').eq('user_id', userId).gte('created_at', startIso).order('created_at', { ascending: false }).limit(300), client.from('attributes').select('*').eq('user_id', userId).order('sort_order'), client.from('seasons').select('*').eq('user_id', userId).eq('status', 'Ativa').maybeSingle(), client.from('rewards').select('*').eq('user_id', userId).eq('is_active', true).order('cost'), client.from('weekly_reviews').select('*').eq('user_id', userId).order('week_start', { ascending: false }).limit(8) ]); const firstError = [profile, missions, projects, journey, checkin, dailyCheckins, focus, routines, completions, activity, attributes, season, rewards, weeklyReviews].find((result) => result.error)?.error; if (firstError) throw firstError; return { profile: profile.data as PlayerProfile | null, missions: (missions.data ?? []) as NexusMission[], projects: (projects.data ?? []) as NexusProject[], journey: journey.data as NexusWorkspace['journey'], checkin: checkin.data as DailyCheckin | null, dailyCheckins: (dailyCheckins.data ?? []) as DailyCheckin[], focusSessions: (focus.data ?? []) as FocusSession[], routines: (routines.data ?? []) as Routine[], routineCompletions: (completions.data ?? []) as RoutineCompletion[], activity: (activity.data ?? []) as NexusWorkspace['activity'], weeklyReviews: (weeklyReviews.data ?? []) as WeeklyReview[], attributes: (attributes.data ?? []) as NexusWorkspace['attributes'], season: season.data as NexusWorkspace['season'], rewards: (rewards.data ?? []) as NexusWorkspace['rewards'] } }
export async function createMission(userId: string, input: Pick<NexusMission, 'title'> & Partial<NexusMission>) { const client = requireClient(); const { data, error } = await client.from('missions').insert({ ...input, user_id: userId, title: input.title.trim() }).select('*').single(); if (error) throw error; return data as NexusMission }
export async function updateMission(userId: string, missionId: string, patch: Partial<Pick<NexusMission, 'status' | 'priority' | 'rank' | 'context' | 'due_at' | 'project_id' | 'attribute_id'>>) { const client = requireClient(); const { data, error } = await client.from('missions').update({ ...patch, updated_at: new Date().toISOString() }).eq('user_id', userId).eq('id', missionId).select('*').single(); if (error) throw error; return data as NexusMission }
export async function createProject(userId: string, input: Pick<NexusProject, 'name'> & Partial<NexusProject>) { const client = requireClient(); const { data, error } = await client.from('projects').insert({ user_id: userId, status: 'Ativo', priority: 'Média', progress: 0, ...input, name: input.name.trim() }).select('*').single(); if (error) throw error; return data as NexusProject }
export async function completeMissionAtomic(missionId: string) { const client = requireClient(); const { data, error } = await client.rpc('complete_mission', { p_mission_id: missionId }); if (error) throw error; return data as { ok: boolean; already_completed: boolean; xp_awarded: number; coins_awarded: number; profile_xp: number; profile_coins: number; level: number } }
export async function saveDailyCheckin(userId: string, input: Partial<Omit<DailyCheckin, 'id' | 'user_id' | 'checkin_date'>>) { const client = requireClient(); const { data, error } = await client.from('daily_checkins').upsert({ user_id: userId, checkin_date: localDate(), ...input, updated_at: new Date().toISOString() }, { onConflict: 'user_id,checkin_date' }).select('*').single(); if (error) throw error; return data as DailyCheckin }
export async function startFocusSession(userId: string, plannedMinutes: number, label?: string, missionId?: string | null) { const client = requireClient(); const { data, error } = await client.from('focus_sessions').insert({ user_id: userId, planned_minutes: plannedMinutes, label: label || null, mission_id: missionId || null }).select('*').single(); if (error) throw error; return data as FocusSession }
export async function finishFocusSession(userId: string, sessionId: string, actualMinutes: number, distractionCount = 0) { const client = requireClient(); const { data, error } = await client.from('focus_sessions').update({ status: 'completed', ended_at: new Date().toISOString(), actual_minutes: actualMinutes, distraction_count: distractionCount, updated_at: new Date().toISOString() }).eq('id', sessionId).eq('user_id', userId).select('*').single(); if (error) throw error; return data as FocusSession }
export async function toggleRoutineCompletion(_userId: string, itemId: string, completed: boolean) { const client = requireClient(); const { data, error } = await client.rpc('set_routine_item_completion', { p_item_id: itemId, p_completed: completed }); if (error) throw error; return data as { ok: boolean; completed: boolean; xp_delta: number } }
export async function updateProject(userId: string, projectId: string, patch: Partial<Pick<NexusProject, 'status' | 'priority' | 'progress' | 'next_action' | 'due_at'>>) { const client = requireClient(); const { data, error } = await client.from('projects').update({ ...patch, updated_at: new Date().toISOString() }).eq('user_id', userId).eq('id', projectId).select('*').single(); if (error) throw error; return data as NexusProject }
export async function redeemReward(rewardId: string) { const client = requireClient(); const { data, error } = await client.rpc('redeem_reward', { p_reward_id: rewardId }); if (error) throw error; return data as { ok: boolean; coins_remaining: number; reward: string } }
export async function saveWeeklyReview(userId: string, review: Omit<WeeklyReview, 'id' | 'user_id'>) { const client = requireClient(); const { data, error } = await client.from('weekly_reviews').upsert({ user_id: userId, ...review, updated_at: new Date().toISOString() }, { onConflict: 'user_id,week_start' }).select('*').single(); if (error) throw error; return data as WeeklyReview }
